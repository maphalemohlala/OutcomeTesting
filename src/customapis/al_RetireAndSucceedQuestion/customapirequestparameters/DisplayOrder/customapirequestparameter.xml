<customapirequestparameter uniquename="DisplayOrder">
  <description default="Optional. Integer order for the new version. Absent carries the current value forward. Display order is frozen on the version (AD-015), so reordering is a new version, never an in-place update.">
    <label description="Optional. Integer order for the new version. Absent carries the current value forward. Display order is frozen on the version (AD-015), so reordering is a new version, never an in-place update." languagecode="1033" />
  </description>
  <displayname default="Display order">
    <label description="Display order" languagecode="1033" />
  </displayname>
  <iscustomizable>1</iscustomizable>
  <isoptional>1</isoptional>
  <name>DisplayOrder</name>
  <type>10</type>
</customapirequestparameter>
