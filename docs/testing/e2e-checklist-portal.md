# Outcome Testing — end-to-end UAT checklist: Power Pages portal

| ID | Page | Action | Expected outcome | Functionality/feature tested | Result |
|---|---|---|---|---|---|
| PRT-001 | Home | Open the site signed out. | Sign-in is required and no case data is visible. | Anonymous access blocked | Pass — every page redirects to Entra sign-in; no case data in the response. |
| PRT-002 | Home | Attempt to register a new account. | Self-registration is refused. | Registration disabled | Pass — OpenRegistrationEnabled and LocalLoginEnabled are both false; Registration/Enabled stays true only so contact mapping runs. |
| PRT-003 | Home | Sign in as a Tax Reviewer. | The primary navigation shows My Work, Cases, Tax reviews, AQS reviews and Remediation. | Authenticated navigation | Pass. Driven in the portal as Simunye Radingwana, a real second identity holding the reviewer web roles and - confirmed, not assumed - **not** a Dataverse System Administrator (Basic User + Outcome Testing App User only), so nothing here is an admin bypassing a gate invisibly. The primary navigation offers exactly **My Work, Cases, Tax reviews, AQS reviews, Remediation** (plus the account menu, Profile and Sign out) and all four scoped pages return HTTP 200. Honest scope note: she held the Tax Reviewer role together with AQS Reviewer, Adviser Remediation and T&C Supervisor at this point, so what is proved is that a Tax Reviewer's five items are all present and reachable, not that Tax Reviewer ALONE yields exactly these five. The complementary direction is proved on PRT-043 and PRT-076, where roles were taken away one at a time and the pages went to 403. |
| PRT-004 | Home | Sign in as an Adviser Remediation user. | Only the pages that role is granted are offered. | Role-based navigation | **Partial - the navigation is right, the landing page is not.** Driven in the portal as a real second identity: the Dev Account (`svc.automate.aq-dev`), holding **only** the `AL Portal - Adviser Remediation` web role and no System Administrator, with `REM-900000001-2` on case 900000001 assigned to its contact. Gate 0 had to be opened first - a private Power Pages site refuses everyone not on Manage access, and the account stopped bypassing it the moment System Administrator came off, which is the portal-access runbook's own warning that an admin account is a useless test subject. The primary navigation offers exactly **My Work, Cases, Remediation** (plus Profile and Sign out): no Tax reviews, no AQS reviews. That half is correct. But **My Work itself advertises work this role cannot open**: a counter reading "REVIEWS TO COMPLETE 3" linking to `/tax-reviews`, and three rows - 910000009, 900000006, 900000003 - each linking to `/review?id=...` and each showing case reference, client name and adviser name. Every one of those links returns **HTTP 403**. The page filters by ASSIGNMENT and not by role, so an account that was a reviewer keeps its review queue on the landing page after the role is taken away; it simply cannot open any of it. Nothing is readable that the server would not also serve - the refusals are real - but "only the pages that role is granted are offered" is not met, and the first thing this adviser sees is three pieces of work that are not theirs. |
| PRT-005 | Home | Sign in as a contact with no web role. | No case, review, response or remediation data is readable. | No global read for Authenticated Users | Pass, and tested as a real signed-in contact rather than a signed-out browser - which is the distinction this row turns on. Driven by taking the single web role OFF the Dev Account (`svc.automate.aq-dev`), holding only the `AL Portal - Adviser Remediation` web role and no System Administrator mid-session (the same experiment as PRT-106) and reloading, so the contact stayed AUTHENTICATED with no web role, which is exactly the state described. Every page then returned **HTTP 403** and the site's Access Denied page: `/`, `/cases`, `/remediation`, `/remediation?case=...` addressed straight at the case they had been working on minutes before, `/tax-reviews` and `/profile`. Checked the RESPONSE BODIES, not just the status codes: no case reference, no client name, and none of the adviser response text that was on screen before the role came off. The primary navigation collapsed to nothing - only the account button and Sign out remained. Worth recording that the refusal page enumerates what it did resolve: "You are signed in as Dev Account (svc.automate.aq-dev@...). Your roles on this site: Authenticated Users." So `Authenticated Users` alone grants no case, review, response or remediation data, which is the global-read question this row exists to answer. |
| PRT-006 | Home | Review the Outcome Testing summary cards. | Counts match the underlying lists for that user's scope. | Home card accuracy | Pass - the four cards matched the scoped lists once the render cache expired (0 before allocation, 1 after). |
| PRT-007 | Home | Click a summary card. | The scoped list opens, not the unscoped one. | Card drill-through scope | Pass - each card links to its scoped list (/cases?mine=1, /tax-reviews), not the unscoped one. |
| PRT-008 | My Work | Open the page as a reviewer with allocations. | "Reviews assigned to you" lists only their own reviews. | My Work scoping | Pass - listed only the signed-in user’s review on 900000003; Simunye’s review on 910000009 was not shown. |
| PRT-009 | My Work | Open the page as a reviewer with no allocations. | An empty state distinct from "no cases found" is shown. | Empty worklist state | Pass - "Nothing assigned to you / Reviews appear here once a team lead allocates one to you", distinct from the case-list empty state. |
| PRT-010 | My Work | Compare the My cases tile with the Cases list. | The tile count and the scoped list agree. | Tile and list consistency | Pass - My cases tile 1 agreed with /cases?mine=1 returning 1. |
| PRT-011 | My Work | Open a review from the list. | The correct review opens for the correct case. | Review navigation | Pass - the row linked to /review?id=77c58a05 and opened the Tax review for case 900000003. |
| PRT-012 | My Work | Hold both a Tax and an AQS review on one case. | The case is listed once, not twice. | Duplicate row prevention | |
| PRT-013 | Cases | Open the page. | Columns show Case reference, Client, Adviser, Route, Tax checker, AQS checker, Case type, Status, Outcome, Date of meeting and Due. | Case table column set | Pass - all eleven columns render in the order specified. |
| PRT-014 | Cases | Open an unallocated Tax-then-AQS case. | Both checker columns read "Not yet allocated". | Checker empty state, unallocated | Pass (after the F4 fix) - an unallocated Tax then AQS case reads "Not yet allocated" in both checker columns. Failed before the fix: both cells rendered empty. |
| PRT-015 | Cases | Open an AQS-only case. | The Tax checker column reads "No check of this type". | Checker empty state, not required | Pass (after the F4 fix) - AQS-only case 900000001 reads "No check of this type" in the Tax checker column. |
| PRT-016 | Cases | Compare a checker cell with the same case in the Code App. | Both surfaces show the same name or the same empty state. | Cross-surface consistency | Pass (after the F4 fix) - the Code App and the portal now show the same name or the same empty state; verified on 900000001, 900000003 and 900000004. |
| PRT-017 | Cases | Filter by reference, client, adviser, status, type, route and outcome. | Only matching cases are listed. | Case filtering | Pass - reference, client, adviser, status, type, route, outcome and date all filter, and combine correctly (ref+route returned 6). |
| PRT-018 | Cases | Filter by priority. | Filtering works although the Priority column is not displayed. | Filter independent of column set | Pass - the priority filter applies although the column is hidden; no seeded case carries a priority so the positive match is unproven. |
| PRT-019 | Cases | Apply filters matching nothing. | "No cases match these filters" is shown. | Filtered empty state | Pass - "No cases match these filters" with "Widen or clear the filters to see more cases." |
| PRT-020 | Cases | Open the page with `?mine=1`. | Only cases carrying the user's own review are listed. | My cases scope | Pass - ?mine=1 returned only the case carrying the user’s own review. |
| PRT-021 | Cases | Page through more than one page of results. | Paging is server-side and the count agrees. | Pagination | Partial - only 12 cases exist, so paging past page 1 is untested; page=2 is empty and page=abc and page=-5 clamp to page 1 without error. |
| PRT-022 | Cases | Check the Outcome column on an ungraded case. | "Not yet graded" is shown rather than a blank. | Outcome empty state | Pass - "Not yet graded" is shown, and outcome=none returns all 12. |
| PRT-023 | Cases | Check the Outcome column on a Tax-only graded case. | The Tax grade is shown labelled as Tax. | Tax grade display | Pass. Case **900000003** is on the Tax then AQS route with the Tax leg submitted and the AQS leg still Assigned, and the Cases list renders its Outcome cell as **"Tax: Pass"** - the grade carried, and explicitly labelled with the discipline it came from, so a part-graded case cannot be mistaken for a finished one. Every other ungraded case in the same column reads "Not yet graded", so the label is doing work rather than being decoration. |
| PRT-024 | Cases | Check a regraded case. | The final grade is shown, not the initial one. | Regrade display | Pass. Case **900000001** was regraded during this run (PRT-091: initial Pass with issues, final outcome Pass) and the Cases list Outcome cell reads **"Pass"** - the FINAL grade. Confirmed against the data rather than inferred: `al_outcome` `OUT-900000001-2` holds `al_initialoutcome` 120910701 (Pass with issues) and `al_finaloutcome` 120910710 (Pass), so the list is showing the second of the two and BR-007's requirement that both are kept is unaffected. Worth recording that the case DETAIL page shows "Outcome Pass with issues" in its remediation section - the grade the remediation was raised against - so the two surfaces show different values by design, each correct in its own context. |
| PRT-025 | Cases | Check an unrouted case. | "Not routed" is shown and the case still appears. | Unrouted case visibility | Pass, on a fixture built for it. No case in DEV was unrouted, so `al_reviewrouteid` was cleared on spare case **910000007** (the case table carries no plug-in steps, so the write is inert) and restored to AQS only afterwards. With no route the Cases list shows the case **still present**, with Route reading **"Not routed"** and the checker columns falling back to "Not yet allocated" - so an unrouted case is visible and labelled rather than silently dropped from the list, which is what this row exists to prevent. Note the list is server-cached: the first two reloads after the change still showed "AQS only", which is the same render cache recorded on PRT-090, and is worth knowing before reading a stale cell as a bug. |
| PRT-026 | Case detail | Open a case from the list. | Case header, checks and remediation summary are shown. | Case detail rendering | Pass - case header, checklist items, case management, review progress and remediation all render. |
| PRT-027 | Case detail | Check the IO reference. | It shows ClientRef, not TaskID. | ClientRef as IO reference | Pass (after the F6 fix) - 900000001 reads 90000001-90000101 and 900000003 reads 90000003-90000103, the al_ioreference values. Failed before the fix: it showed the case reference. |
| PRT-028 | Case detail | Check the Paraplanner field. | It shows the extract's "Assigned by" value. | Assigned by mapped to Paraplanner | Pass - Paraplanner shows "Simunye Radingwana", the extract’s Assigned by value. |
| PRT-029 | Case detail | Check the Tax Checker and AQS Checker fields. | Each shows its own checker, or the correct empty state. | Separate checker fields | Pass (after the F4 fix) - 900000003 reads the Tax checker’s name and "Not yet allocated" for AQS; 900000001 reads "No check of this type" and the AQS checker’s name. |
| PRT-030 | Case detail | Open a case with no Tax review. | The Tax Checker reads "No check of this type". | Checker state from reviews | Pass (after the F4 fix) - AQS-only case 900000001 reads "No check of this type" for the Tax checker. |
| PRT-031 | Case detail | Edit header details as the assigned checker. | The edit is accepted and audited. | Checker header edit | **Fail as written - the portal has no header edit at all, so this cannot be exercised here.** Driven in the portal as Simunye Radingwana, a real second identity holding the reviewer web roles and - confirmed, not assumed - **not** a Dataverse System Administrator (Basic User + Outcome Testing App User only), so nothing here is an admin bypassing a gate invisibly. `OT Case Detail` renders no form: the whole page carries exactly one control, the navigation toggle. And the permission underneath matches - a hand-made PATCH on `/_api/al_outcomecases(<id>)` as the signed-in reviewer returns **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate` - "You don't have permission to update al_outcomecase table"** - on her OWN assigned case as much as on anyone else's, so the read-only page is backed by a table permission and is not merely a UI omission. Header editing is a Code App function (`al_UpdateCaseDetails`), which is where the equivalent app rows live. Recorded as a scope correction for the project owner rather than a defect: nothing is broken, but this row cannot pass on the portal as it is written. This row asks for an edit to be ACCEPTED and audited, which no portal path offers. |
| PRT-032 | Case detail | Edit header details as a checker on a different case. | The edit is refused server-side. | Checkers edit only assigned cases | Pass, proved server-side. Driven in the portal as Simunye Radingwana, a real second identity holding the reviewer web roles and - confirmed, not assumed - **not** a Dataverse System Administrator (Basic User + Outcome Testing App User only), so nothing here is an admin bypassing a gate invisibly. There is no edit form to misuse, so the request the form would make was issued by hand: a PATCH on `/_api/al_outcomecases(<id>)` changing `al_clientname` on case **900000006**, which she is not a checker on. **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate`**. The refusal is a Power Pages table permission, not UI code, and it is absolute rather than assignment-scoped - the identical request against her OWN assigned case 900000004 is refused the same way. |
| PRT-033 | Case detail | Change the case id in the URL to another case and edit it. | The edit is refused. | URL tampering blocked | Pass. The same hand-made PATCH aimed at a DIFFERENT case id than the one the page was showing - the URL-tampering shape this row describes - returned **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate`** and wrote nothing. Swapping the id changes nothing about the answer, because the permission is on the table and not on the id, which is the stronger of the two possible reasons for this row to pass. |
| PRT-034 | Case detail | Set "Date of meeting - Client contact" to a future date. | The save is refused. | Future date rejection | Pass — the same server-side refusal as APP-030, raised by the command not the page. |
| PRT-035 | Case detail | Set the date of meeting after the due date. | The save is refused. | Date of meeting vs due date | **Fail as written - the portal has no header edit at all, so this cannot be exercised here.** Driven in the portal as Simunye Radingwana, a real second identity holding the reviewer web roles and - confirmed, not assumed - **not** a Dataverse System Administrator (Basic User + Outcome Testing App User only), so nothing here is an admin bypassing a gate invisibly. `OT Case Detail` renders no form: the whole page carries exactly one control, the navigation toggle. And the permission underneath matches - a hand-made PATCH on `/_api/al_outcomecases(<id>)` as the signed-in reviewer returns **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate` - "You don't have permission to update al_outcomecase table"** - on her OWN assigned case as much as on anyone else's, so the read-only page is backed by a table permission and is not merely a UI omission. Header editing is a Code App function (`al_UpdateCaseDetails`), which is where the equivalent app rows live. Recorded as a scope correction for the project owner rather than a defect: nothing is broken, but this row cannot pass on the portal as it is written. The date-of-meeting rule is real (it is enforced by `al_UpdateCaseDetails`), but there is no portal field to enter a date into, so the rule cannot be reached from here. |
| PRT-036 | Case detail | Check the due date on a newly imported case. | It is three days after the upload. | Due date default | Pass. Every case imported on 2026-09-20 carries a due date of **23 Sep 2026** - three days after the upload, as the row requires. Checked against `createdon` on the case record (2026-09-20T13:36:42Z) rather than against the visible list alone. Scope note: three CALENDAR days is what the data shows; 20 to 23 September spans no weekend, so this run cannot distinguish calendar days from working days. A case imported on a Thursday or Friday would tell them apart and none exists. |
| PRT-037 | Case detail | Change the due date as a role without the grant. | The change is refused. | Due date edit permission | **Fail as written - the portal has no header edit at all, so this cannot be exercised here.** Driven in the portal as Simunye Radingwana, a real second identity holding the reviewer web roles and - confirmed, not assumed - **not** a Dataverse System Administrator (Basic User + Outcome Testing App User only), so nothing here is an admin bypassing a gate invisibly. `OT Case Detail` renders no form: the whole page carries exactly one control, the navigation toggle. And the permission underneath matches - a hand-made PATCH on `/_api/al_outcomecases(<id>)` as the signed-in reviewer returns **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate` - "You don't have permission to update al_outcomecase table"** - on her OWN assigned case as much as on anyone else's, so the read-only page is backed by a table permission and is not merely a UI omission. Header editing is a Code App function (`al_UpdateCaseDetails`), which is where the equivalent app rows live. Recorded as a scope correction for the project owner rather than a defect: nothing is broken, but this row cannot pass on the portal as it is written. Trivially satisfied in the sense that the change is refused - but refused because NOBODY can edit a case header through the portal, not because the `case.duedate` grant was evaluated, so it proves nothing about the permission this row names. |
| PRT-038 | Case detail | Open a case id that does not exist. | A not-available message is shown, not an error page. | Invalid id handling | Pass on the row, with a defect found next door (F48). A case id that does not exist renders the site's own empty state - **"Case not available - This case does not exist, or you do not have permission to view it."** - inside the normal page chrome, not an error page and not a stack trace. The wording is deliberately ambiguous between the two reasons, which is right: confirming that a case exists but is not yours is itself a disclosure. Omitting the id entirely gives a different and equally clean state, "No case selected - Open a case from the case list to see its detail." **But a MALFORMED id** (`?id=not-a-guid`) renders **`Liquid error: Exception has been thrown by the target of an invocation.`** above the friendly message - a raw platform exception string on the page. The friendly state still appears underneath and nothing leaks beyond the exception text, so the row itself passes; the leak is recorded as F48. |
| PRT-039 | Case detail | Open an Intelligent Office document reference. | The reference opens in IO and nothing is stored locally. | Document reference handling | **Partial.** The Intelligent Office reference IS carried and displayed - case detail shows "IO reference 90000003-90000103" - and the "nothing is stored locally" half is satisfied completely: it is a reference string on the case record, there is no document, no attachment and no file anywhere in the portal for it. But the reference is rendered as **plain text, not a link**: there is no anchor on the case detail page pointing at IO, so "the reference opens in IO" does not happen - a person has to copy the reference and go to IO themselves. Whether that is a gap or the intended design is a question for the project owner: a deep link needs an IO base URL that this environment has no setting for, so it may be deliberate. |
| PRT-040 | Case detail | Check the Tax notes panel. | The Tax checker's remedial text is visible to the adviser and AQS checker. | Tax notes visibility | **Fail - finding F49.** The Tax notes panel exists, is correctly written, is included on both pages it should be (the remediation page for the adviser, the review detail page for the AQS checker), and still does not reach the AQS checker when they need it. The include is guarded by `{% if rv.al_submittedon and rv.al_reviewtype.value != 120910200 %}` - and `rv` is the review BEING VIEWED. So the panel appears on an AQS review only once the AQS checker has already submitted it. Proved on case **900000003**, which carries a submitted Tax review whose Q-TAX-04 "Tax Remedial" answer is stored (`al_answerrichtext` = `<strong>Bold kept</strong> plain text link`): opening that case's AQS review, which is Assigned and not yet submitted, renders **no Tax notes panel at all**. The panel's own fetch already restricts itself to SUBMITTED Tax reviews, so the guard on the viewing review's submission date is guarding the wrong thing and defeats the panel's stated purpose - "what was missing is the AQS checker's view of it, which AD-020 filters out of their form by design". Not changed here: when a checker may see another discipline's notes is a project-owner decision (2026-09-19), not an obvious bug, and the fix is one condition. |
| PRT-041 | Case detail | Check the remediation summary on a failed case. | Actions, statuses and due dates are listed. | Remediation summary | Pass. Case **900000001**, which failed as Pass with issues and has been through a full remediation, shows a Remediation and escalation section on the case detail page carrying the header "Outcome Pass with issues" and a table with **No., Issue / fail reason, Remedial action, Owner, Target date, Status, Age and Sign-off**. The live row lists the adviser's response text in full, Owner **Dev Account**, Target date **02 Oct 2026**, Status **Completed** and Sign-off **Approved 21 Sep 2026**, under a discipline heading reading "AQS check - every action approved, closed". So actions, statuses and due dates are all listed, and the sign-off state is carried with them. |
| PRT-042 | Tax reviews | Open the page as a Tax Reviewer. | Tax reviews assigned to that user are listed. | Tax review list scoping | Pass - the Tax review allocated to the signed-in user is listed. Note: reviews belonging to other users are listed too, which this identity (Administrators) may be entitled to; see PRT-043. |
| PRT-043 | Tax reviews | Open the page as an AQS Reviewer. | No Tax reviews of other users are listed. | Cross-discipline scoping | Pass - and it only became a clean test after the roles were stripped properly, which is worth recording because the first attempt looked like a defect. With Tax Reviewer removed but **T&C Supervisor still held**, `/tax-reviews` returned HTTP 200 and listed all four Tax review instances including three belonging to other people. That is not a scoping failure: T&C Supervisor legitimately sees across disciplines, so the account was simply not "an AQS Reviewer" as this row means it. Removing T&C Supervisor as well, leaving **AQS Reviewer (+ Adviser Remediation) only**, `/tax-reviews` returns **HTTP 403** and the site's Access Denied page - no Tax reviews of anybody are listed, which is stronger than the row asks. Web role REMOVAL takes effect on the very next request (PRT-106), so no re-sign-in was needed between the two states. |
| PRT-044 | Tax reviews | Claim an unassigned case from the queue. | The case is allocated to the claimant and a Tax review is created. | Case claim | Not run - the Tax reviews page exposes no claim control; only the AQS queue offers "Run checks". Claiming was exercised at PRT-075. |
| PRT-045 | Tax reviews | Claim a case already claimed by someone else. | The claim is refused. | Claim contention | Pass, refused server-side with words a person can act on. Case **910000004** was claimed through the portal first (see PRT-046), moving it to Assigned. Replaying the claim - the real request the page makes, a PATCH on the signed-in contact carrying `al_claimrequest`, with a valid `__RequestVerificationToken` - returns **HTTP 400, "PRECONDITION: This case is Assigned, not in the queue, so it cannot be picked up. Someone may have taken it already."** That is the contention message, it names the state, and it reaches the browser clean - prefix first, no plug-in class name - which is the refusal-packaging fix from earlier in this run working in a path it was not tested against. |
| PRT-046 | Tax reviews | Claim a case on a route that owes no Tax check. | No Tax review is created. | Route-driven review creation | Pass, and checked in the data rather than on the screen. Case **910000004** is on the **AQS only** route, which owes no Tax check. Claiming it through the portal ("Run checks", with a clear confirmation step first - "The case will be assigned to you and will no longer be available for anyone else to pick up") created **exactly one** `al_reviewinstance`: type 120910201 AQS, sequence 2, assigned to the claimer. **No Tax review was created**, which is the whole of this row. The case moved Queued -> Assigned and `al_aqscheckername` was filled with the claimer's name. The sequence number is 2 rather than 1, which is correct and worth noting - the AQS leg keeps its position in the route even when the Tax leg it would have followed is never raised. |
| PRT-047 | Tax reviews | Open the page with no assigned reviews. | An empty state is shown. | Empty review list | Pass - the Remediation page shows an empty state rather than an empty table. |
| PRT-048 | Review | Open a Tax review. | The checklist for the case's checklist version is shown. | Checklist versioning | Pass - the V8 checklist rendered with its Tax sections, fail points, outcome and accountability blocks. |
| PRT-049 | Review | Open a case imported under an earlier checklist version. | The earlier questions are shown, not the current ones. | Checklist version pinning | Partial - the review for case 900000001 rendered "Checker Checklist V5 Draft" while 900000003 rendered V8, so an older version does render; the version each case was imported under was not independently confirmed. Note: V5 is in Draft status. |
| PRT-050 | Review | Answer each response type on the checklist. | Text, date, choice, multi-choice and rich text all save. | Response types | Partial - choice answers save and persist as al_response rows; text, date, multi-choice and rich text not yet exercised. |
| PRT-051 | Review | Enter text in the "Tax Remedial" field. | Formatting is preserved and the text saves. | Tax Remedial rich text | Partial - the text saves, but a <b> tag injected into the editor did not survive the sanitiser, and the toolbar path was not exercised, so "formatting is preserved" is unproven. |
| PRT-052 | Review | Paste disallowed markup into a rich text answer. | The text is kept and the markup is removed. | Answer sanitising | Pass - "Bold kept" survived; no script, onerror, javascript: or alert( reached al_response. Markup stripped, text kept. |
| PRT-053 | Review | Save an answer, then reload the page. | The saved answer is shown. | Answer persistence | FAIL, mitigated (F5) - the saved answers still do not render until the AD-094 cache expires, which is the platform’s behaviour and not ours to change. The page now says so: "One answer you saved is not shown below yet. It is saved... Do not enter it again." Verified across three reloads, including that the notice stays hidden once the cache catches up. |
| PRT-054 | Review | Answer a question on a review assigned to someone else. | The save is refused server-side. | Response scope enforcement | Pass — al_SubmitReview refused a non-assigned caller: UNAUTHORIZED. |
| PRT-055 | Review | Change the review id in the URL to another user's review. | Access or write is refused. | Review URL tampering | Pass on "write is refused", which is what the row requires ("Access **or** write is refused"). Opening another user's review by id - the AQS review on case 900000003, assigned to the Dev Account - DOES render, read-only, with an explicit banner: **"This review is assigned to someone else. You can read it, but only the assigned reviewer can record or change answers on it."** That read is deliberate and is not a leak - it is a reviewer seeing a colleague's review in a discipline they hold. The write half was proved rather than trusted to the banner: a hand-made PATCH on `/_api/al_reviewinstances(<that id>)` setting `al_submitrequested` returns **HTTP 403 "You don't have permission to update al_reviewinstance table"**, a Power Pages table permission and not UI code. |
| PRT-056 | Review | Submit with mandatory questions unanswered. | Submission is refused and the missing items are named. | Mandatory answer validation | Pass - refused and named them: "4 of 4 required questions are unanswered: Q-FQTAX-01, Q-TAX-01, Q-TAX-02, Q-FQTAX-03." |
| PRT-057 | Review | Set an AQS grade of Pass. | "Primary root cause" is hidden and not required. | Conditional root cause, Pass | Pass - with the grade set to Pass, Primary root cause stays hidden and is not required. |
| PRT-058 | Review | Set an AQS grade other than Pass. | "Primary root cause" is shown and required. | Conditional root cause, non-Pass | Pass - a non-Pass grade reveals Primary root cause, and reverting to Pass hides it again. |
| PRT-059 | Review | Submit a non-Pass grade with no root cause. | Submission is refused server-side. | Root cause enforcement | Pass - refused server-side and named it: "1 of 36 required questions are unanswered: Q-GR-02." |
| PRT-060 | Review | Mark a Suitability core check Insufficient Evidence. | The outcome is restricted to Insufficient Evidence or Potential Harm. | Suitability outcome restriction | Pass. On the AQS checklist for case 910000004, marking the Suitability core check **"Report explains why the recommendation is suitable"** as *Insufficient evidence* immediately changed the grading control: PASS and PASS WITH ISSUES became **disabled** and only INSUFFICIENT EVIDENCE and POTENTIAL HARM remained selectable. Checked the radios' own `disabled` state rather than appearance, and checked before as well as after - all four were selectable beforehand - so the restriction is caused by the answer and is not the page's resting state. |
| PRT-061 | Review | Attempt to submit that review graded Pass. | Submission is refused server-side. | Suitability restriction enforcement | Pass, and in a stronger form than the row asks for. The row expects the submission to be refused; what actually happens is that the forged grade is **ignored and recomputed**, which is better. The client-side guard from PRT-060 was deliberately defeated - the disabled attribute removed from the PASS radio in the browser and PASS selected - with the Suitability core check still answered *Insufficient evidence*. The review submitted, and then every downstream record disagreed with the client: `al_outcome` `OUT-910000004-2` recorded `al_initialoutcome` **120910702 Insufficient evidence**, the case moved to **Awaiting Remediation** rather than closing as a pass, a remediation action (`REM-910000004-2-1`) was raised, and the completed-check PDF sent to the para-planner reads **"Advice Quality Grade: Insufficient evidence"**. So the grade is derived server-side from the answers and the client cannot lie about it - the browser's PASS reached nothing. Recorded as a pass against intent with the row's letter flagged: nothing was "refused", so a client that forges a grade gets HTTP success, and anybody reading the row literally would expect an error. |
| PRT-062 | Review | Complete "Who carries this fail" on a failed review. | The accountable person is recorded. | Fail accountability | Pass - "Who carries this fail" was completed and accepted as part of the submitted review. |
| PRT-063 | Review | Submit a failed review with no accountable person. | Submission is refused. | Accountability validation | **Fail - finding F50, and it is worse on the deferred-Tax path than a single review shows.** Case 900000004 was failed on BOTH legs (Tax check Fail, then AQS Potential harm) and came out graded **Potential harm with nobody recorded as carrying it**: the outcome record has `al_fqadviseraccountable`, `al_fqparaplanneraccountable`, `al_aqadviseraccountable` and `al_aqparaplanneraccountable` all **False** and both accountable-contact lookups null. Nothing refused the submission. Two separate causes, both proved. **(1) A recorded judgement is discarded.** The "Who carries this fail" panel WAS used on the Tax leg - `The adviser (Simunye Radingwana)` ticked - and it saved: the Tax review still holds `al_pendingaccountability` = `{"aqAdviser":false,"aqParaplanner":false,"fqAdviser":true,"fqParaplanner":false,...}`. But on a Tax-then-AQS case the Tax submit raises no outcome (it defers, correctly), and `SubmitReviewPlugin` applies accountability by reading `al_pendingaccountability` from **the review being submitted** - the AQS one, which had none. So `ApplyParked` returned false, the flags were never written, and the Tax checker's answer is still sitting on the Tax review, unread. **(2) There is no fallback.** The panel tells the checker "Left alone, the extract names the people the case itself does: the paraplanner for a File Quality fail, the adviser for an Advice Quality fail" - and no such default exists in the code; `ApplyParked` simply returns false and the booleans keep their Dataverse default of False. Either the submission should be refused, as this row expects, or the promised fallback should apply. Neither happens, and the case closes the gap silently. |
| PRT-064 | Review | Submit a Tax review with a Fail grade. | The case proceeds to the AQS check, not to remediation. | Tax fail proceeds to AQS | Pass - completed 2026-09-20, upgrading the earlier Partial. The Fail path the note said was untested has now been run: case 900000006 (Tax then AQS) had Q-TAX-02 answered **Fail** and the Tax review submitted, and the case moved Assigned -> QUEUED for the AQS leg with `al_taxoutcome` Fail stamped and ZERO remediation actions raised. So a Tax fail proceeds to the AQS check rather than to remediation, which is what this row asks. Caveat on the method: submitted through `al_SubmitReview` over the Web API rather than through the portal form, so this proves the rule and the command the portal calls, not the form's own wiring. The same run is recorded as APP-051, and it is what uncovered F38 - before that fix the AQS leg could never be submitted afterwards, so this row's own expectation led to a deadlock one step later. |
| PRT-065 | Review | Submit the Tax review of a Tax-only case. | Remediation or closure follows with no AQS leg. | Tax-only path | Pass, on a fixture built for it. No case in DEV used the **Tax only** route, so spare case **910000006** was moved onto it. Worth recording how the review was created: the Tax reviews page offers **no claim path at all** - unlike AQS reviews, which carries an "Available cases / Run checks" queue - so a Tax review arrives by ALLOCATION, and `al_AssignCase` was used, which is the same command the Code App calls. That allocation created **exactly one** review: Tax, sequence 1. Submitting it as a fail produced the result this row asks for - the case moved to **Awaiting Remediation**, a remediation action `REM-910000006-1` was raised Open, and **no AQS leg was ever created**; the case still carries a single review instance. Note the action code is `REM-910000006-1`, keyed on the Tax review's sequence, where a Tax-then-AQS case produces `REM-<ref>-2-n` keyed on the AQS leg - so the Tax-only case raises its remediation directly rather than deferring, which is the distinction PRT-067 tests from the other side. **One observation for the project owner, not a claim:** this case has **no `al_outcome` row at all**, where every AQS-graded case in DEV has one. If the sign-off and regrade path reads `al_outcome` - it does on the remediation page - it is worth confirming what a Tax-only case is meant to do when it reaches sign-off. Not traced here, so it is flagged rather than raised. |
| PRT-066 | Review | Submit the AQS review of an AQS-only case. | The outcome is recorded and remediation raised where required. | AQS-only path | Pass - submitting a non-Pass AQS review on AQS-only case 900000001 set it to Awaiting Remediation and raised remediation action REM-900000001-2. |
| PRT-067 | Review | Submit the AQS leg where both disciplines failed. | One combined remediation is raised for the case. | Combined remediation | Pass. Driven end to end on case **900000004** (Tax then AQS): the Tax leg was submitted as a **Fail** with the disposition set to Submit to AQS, then the AQS leg was claimed and submitted as **Potential harm**. The intermediate state is the half that proves the design: after the Tax fail, **zero** remediation actions existed and the case went back to **Queued** for an AQS checker - the Tax fail is deferred, not raised. After the AQS submit there is **one outcome record** (`OUT-900000004-2`, initial outcome Potential harm) and one raising event carrying **both disciplines**: the actions are `REM-900000004-2-1` and `REM-900000004-2-2`, both keyed on the AQS leg's sequence, both due 02 Oct 2026, and each describing itself as "Raised automatically when the review was submitted (**Potential harm; Tax check: Fail**)" - naming both gradings. Between them they carry one item from each discipline - the Tax leg's `AML - ID verification issue` and the AQS leg's `Report explains why the recommendation is suitable: Fail` - and both carry BOTH checkers' recorded words merged into one note. So "one combined remediation" is one raising event covering both checks, expressed as one action per numbered issue, which is the agreed form (project owner, 2026-09-10). What this row exists to prevent - a Tax remediation at the Tax submit and a second at the AQS submit - does not happen. |
| PRT-068 | Review | Submit a review and check the para-planner's email. | It arrives with the completed check PDF attached. | Para-planner PDF attachment | Pass. Submitting the AQS review on case 910000004 raised one `al_notification`, event **Review submitted** (120910801), recipient **svc.automate.aq-dev@ascotlloyd.co.uk** - which is the contact the case's para-planner "Dev Account" resolves to, not the adviser and not the checker - status **Sent**, with `al_attachmentname` **"Completed check 910000004.pdf"** and a populated `al_attachmentbody`. So the letter goes to the para-planner and the completed-check PDF really is attached to it rather than merely referenced. |
| PRT-069 | Review | Open that PDF. | Answers appear under their sections and rich text reads as text. | PDF content | Pass, on the actual bytes. The attachment was decoded from `al_attachmentbody` and is a valid PDF (`%PDF-1.4`, 9,292 bytes, 125 text runs). **Answers appear under their sections**: a header block (case reference, IO reference, client, adviser, para-planner, date of meeting, due date, status), then "Why this case was selected", "Checks on this case", and each checklist section as its own heading - AML and CRA checking points, File Quality: AQS, Client Objectives & Information (COBS 9.2), Risk Capacity & Loss, Research & Recommendation Rationale (COBS 9.3), Costs Charges & Value, Suitability Report & Client Communication, Centralised Retirement Proposition, Consumer Duty overlay, Checker judgement and grading - with every question and its answer beneath it, ending with the remedial actions and a note that the document describes the case as it stood at that moment. **Rich text reads as text**: zero text runs in the whole document contain an HTML tag, checked by regex over every run; the earlier PDF for case 900000003 shows the same positively, rendering the stored `<strong>Bold kept</strong> plain text link` as "Bold kept plain text link". The PDF also independently confirms PRT-061 - it records "Advice Quality Grade: Insufficient evidence", not the PASS the browser sent. |
| PRT-070 | Review | Submit a review on a case whose para-planner matches no contact. | The letter is not sent and the reason is recorded. | Unaddressable recipient | Pass, exactly as written. Built the fixture by setting `al_paraplanner` on case **910000005** to **"Ghost Planner"**, a name matching no contact in the environment, then claiming and submitting its AQS review. The para-planner letter was **created and not sent**: `al_notification` event Review submitted, `al_recipientemail` **null**, status **120910812** (not sent), and `al_failurereason` recording **"No recipient address. The event was queued with nobody to send it to, so nothing was sent (OD-030)."** - the reason, in words, on the record. The PDF was still generated and attached, so the completed check exists even though nothing could be delivered, which is the right way round. The scoping is right too: the allocation and remediation letters for the same case still went to the adviser normally, so an unresolvable para-planner suppresses only the para-planner's own letter. |
| PRT-071 | Review | Submit an already submitted review. | The second submission is refused. | Double submission guard | **Pass against intent, with the row's letter flagged.** Tested properly this time - as the **assigned** reviewer on her **own already-submitted** review, which is what the row means; an earlier attempt from an outsider's account was refused for the wrong reason (no write permission at all) and proved nothing. Replaying the submit - the real PATCH the page makes, `al_submitrequested` on `al_reviewinstance`, with a valid token - returns **HTTP 204, not an error**. So it is not "refused". But it is completely **idempotent**, which was checked rather than assumed: `al_submittedon` is unchanged at the original 08:55:07, there is still exactly **one** outcome record, exactly **one** remediation action, and **zero** new notifications - no duplicate para-planner letter and no duplicate PDF. Only `modifiedon` moved, because the PATCH rewrote the flag. Recorded as a pass because nothing can be double-submitted, with the wording flagged: a client replaying a submit is told it succeeded, and a reader of this row would expect a refusal. |
| PRT-072 | Review | Open a review of a case you cannot see. | The "Review not available" empty state is shown, not an error. | Unavailable review state | Pass. A review id that does not exist renders **"Review not available - This review does not exist, or you do not have permission to view it."** in the site's own empty state, inside normal page chrome, with no error page, no stack trace and no platform fault. The wording is ambiguous between the two reasons for the same reason PRT-038's is, and the two empty states are worded consistently with each other. |
| PRT-073 | AQS reviews | Open the page as an AQS Reviewer. | AQS reviews assigned to that user are listed. | AQS review list scoping | Pass. After claiming case 910000004 the AQS reviews page lists it for the signed-in checker: **910000004, Test Client Alpha, Nobody Atall, AQS only, AQS check, sequence 2, Assigned, started 21 Sep 2026, Submitted —**. So a review assigned to that user appears on their own AQS page, alongside the separate "Available cases" queue of unclaimed work. |
| PRT-074 | AQS reviews | Open a case handed back from a Tax check. | The AQS leg is present and openable. | Tax to AQS hand-back | Pass - after the Tax review was submitted, case 900000003 appeared in the AQS queue and was openable. |
| PRT-075 | AQS reviews | Claim an AQS case from the queue. | The case is allocated and an AQS review is created. | AQS claim | Pass - "Run checks" confirmed first ("will no longer be available for anyone else to pick up"), then allocated the case and opened the AQS checklist. |
| PRT-076 | AQS reviews | Open the page as an Adviser. | No AQS reviews are listed. | Role separation | Pass. With every reviewer role removed and **`AL Portal - Adviser Remediation` alone** held, `/aqs-reviews` returns **HTTP 403** and the site's Access Denied page - no AQS reviews are listed, and neither is the "Available cases" queue that would let an adviser claim one. `/tax-reviews` is refused identically. The refusal page names the account it resolved and the roles it found, which is what stops a person chasing a permission problem that is really a wrong-account problem. |
| PRT-077 | Remediation | Open the page as an Adviser with actions assigned. | Only their own remediation actions are listed. | Remediation scoping | **Pass - reasoning corrected and re-proved on 2026-09-21; the first run's evidence was worthless.** Originally recorded as a pass because `/remediation` read "Showing 1 of 1 cases" for the Dev Account (`svc.automate.aq-dev`), holding only the `AL Portal - Adviser Remediation` web role and no System Administrator. That proved NOTHING: at the time case 900000001 carried the only `al_remediationaction` row in the whole environment, so "1 of 1" is what every user alive would have seen, scoped or not. Re-tested properly by creating a second action on case **900000003** - a case this contact is neither the adviser on nor assigned to - and reloading with the cache defeated. The list still read **1 of 1** and still showed only 900000001, so the scoping is real. Two further things the re-test settled. (1) The list's FetchXML carries **no assignee or adviser filter at all** - it selects every `al_outcomecase` with an inner link to `al_remediationaction` - so the scoping is done entirely by the Power Pages table permissions, not by the query. That is the right place for it (security in permissions, not in UI code), but it means the template cannot be read on its own to tell who sees what. (2) The scope is by CASE RELATIONSHIP, not by action assignment: with the one action temporarily reassigned to another contact, this adviser still saw case 900000001, because `al_advisername` still names them. Defensible - the case adviser seeing their own case's remediation is reasonable - but it is not what the row's wording ("only their own remediation actions are listed") literally says, and it is worth the project owner knowing which of the two rules is intended. All probe data was removed afterwards and the assignment restored. |
| PRT-078 | Remediation | Open the page as an Adviser with none assigned. | An empty state is shown. | Empty remediation list | |
| PRT-079 | Remediation | Open an action and respond with evidence. | The response saves and the action moves to awaiting review. | Adviser response | Not run - the only open action belongs to another adviser; needs a signed-in adviser who owns one. |
| PRT-080 | Remediation | Submit a response with no evidence where required. | The submission is refused. | Response validation | Pass - **after F45 was fixed; it could not be tested at all before.** The submission is refused: with the response cell left empty and the three case questions answered, "Save and sign off this remediation" returns **"Record the remedial action for issue 1 before signing off."** and sends nothing. The refusal is client-side for speed and the server holds the same line independently - `CompleteRemediationPlugin` refuses a completion with no recorded response because "a blank response would reach the T&C Manager as an action to attest to with nothing to read" (BR-008). Honest scope note: this row says "no evidence where required" and **there is no evidence field on this action** - `al_evidencereference` exists on the table and is written by other paths, but the portal remediation form collects only the response text and the three answers. So what is proved here is the missing RESPONSE being refused, not a missing evidence reference. |
| PRT-081 | Remediation | Respond to an action assigned to another adviser. | The save is refused server-side. | Remediation scope enforcement | Pass - refused twice server-side: the Web API PATCH returned 403 EntityPermissionWriteIsMissingDuringUpdate, and al_CompleteRemediation returned "UNAUTHORIZED: Only the adviser who owns this remediation action can complete it" even for a caller holding Administrators and T&C Supervisor. |
| PRT-082 | Remediation | Change the action id in the URL to another adviser's action. | Access or write is refused. | Remediation URL tampering | Pass - addressing the action id directly through the Web API was refused with the same 403; the page also renders it read-only ("the rest are read-only"). |
| PRT-083 | Remediation | Check an action's due date. | It is the configured working days from being raised. | Remediation due date | Partial - raised 20 Sep 2026 with a due date of 02 Oct 2026 (8 working days); the configured working-day value was not independently confirmed. |
| PRT-084 | Remediation | Open an overdue action. | It is shown as overdue. | Overdue indication | |
| PRT-085 | Remediation | Have a T&C Supervisor review a response and reject it. | The action reopens for rework and the adviser is notified. | Rework loop | Pass, and the guard in front of it passes too. Driven in the portal as a real T&C Supervisor: Simunye Radingwana, holding only the `AL Portal - T&C Supervisor` web role, on case 900000001 which the adviser (Dev Account) had completed through the portal in PRT-087. **First the refusal:** choosing Rejected with the notes box empty is refused with **"A rejection must say what the adviser needs to put right."** Proved the negative rather than trusting the message - zero `al_signoff` rows, case still Awaiting Sign-off, action `modifiedon` unchanged. That matches the panel's own stated rule, "Notes are required when rejecting: the adviser needs to know what to put right (BR-008), and a rejection returns the actions to them." **Then the rejection itself**, with notes: the page answered "Rejected, recorded against 1 remedial action. The actions go back to the adviser to put right." Dataverse confirms every part of that. An `al_signoff` row (`SO-20260921081209541-5cadc951`) with decision **120910721 Rejected**, `al_signedbyname` **Simunye Radingwana**, `al_signedbycontactid` her contact, and the notes stored in full. The case moved **Awaiting Sign-off -> Awaiting Remediation** (120910589 -> 120910587) and `REM-900000001-2` went back to **In Progress** (120910601), so the work really does return to the adviser rather than merely being marked down. `al_clockstartedon` was set to the moment of rejection, which is **OD-018's restarted ten-working-day clock** actually firing - and the UI surfaces it afterwards as "1 working day (reworked; previous period 1 working day)", so the adviser can see both periods. The adviser's response text was **kept, not wiped**, which is right: they need to see what they wrote in order to correct it. The letter went to the **adviser** - `al_notification` event **Sign-off rejected** (120910804), stored template `SIGNOFF-REJECTED`, recipient `svc.automate.aq-dev@...` - and not to the supervisor, which is the correct direction. One observation for the project owner, not a defect: a returned action keeps `al_completerequested` = true and its old `al_completedon`. Nothing reads them to decide anything - the editable cell keys off `al_actionstatus`, which is correct - but they are stale on an action that is no longer complete. |
| PRT-086 | Remediation | Have a T&C Supervisor approve a response. | The action completes. | Remediation approval | Pass. Driven in the portal as a real T&C Supervisor: Simunye Radingwana, holding only the `AL Portal - T&C Supervisor` web role, on case 900000001 which the adviser (Dev Account) had completed through the portal in PRT-087. With the action back at Completed and the case at Awaiting Sign-off, choosing **Approved** with notes produced "Approved, recorded against 1 remedial action. The final outcome is recorded separately, on this page, once the case reaches its recheck." Confirmed in Dataverse: a second `al_signoff` row (`SO-20260921081434220-4ce01750`), decision **120910720 Approved**, signed by **Simunye Radingwana** at 08:14:34, notes stored. The approval letter went to the **adviser** - event **Sign-off approved** (120910803), recipient `svc.automate.aq-dev@...`. Both sign-off rows survive side by side, the rejection and then the approval, so the history of a reworked remediation is legible rather than overwritten. |
| PRT-087 | Remediation | Complete every action on a case. | The case moves to Awaiting Sign-off and the T&C Manager is notified. | Sign-off due notification | Pass, end to end, and it is the row that proves the adviser's half of BR-008 works at all. Driven in the portal as a real second identity: the Dev Account (`svc.automate.aq-dev`), holding **only** the `AL Portal - Adviser Remediation` web role and no System Administrator, with `REM-900000001-2` on case 900000001 assigned to its contact. Gate 0 had to be opened first - a private Power Pages site refuses everyone not on Manage access, and the account stopped bypassing it the moment System Administrator came off, which is the portal-access runbook's own warning that an admin account is a useless test subject. Case 900000001 carried exactly one action. Through the portal form: the remedial action written into the response cell, Client contact required **No**, Recheck required **Yes**, Changes the advice **No**, then "Save and sign off this remediation". The page answered "Signed off. 1 remedial action is recorded against your three answers and sent to your supervisor." Confirmed in Dataverse: `REM-900000001-2` is **Completed** at 07:36:34Z with the response text stored and all three answers recorded (`al_recheckrequired` 120910796 = Yes), and the case moved **Awaiting Remediation -> Awaiting Sign-off**. The notification half is confirmed too, and it went further than queued: one `al_notification` row, event **SignoffDue**, status **Sent**, recipient the mapped T&C Manager - and the `email` row for it exists, subject "Sign-off needed on case 900000001". Worth recording that the sign-off-due letter goes to the mapped T&C MANAGER and not to the adviser, so an adviser with an unroutable address does not suppress it (which is what PRT-088 asks separately). |
| PRT-088 | Remediation | Complete every action where the adviser is unmapped. | The case still reaches Awaiting Sign-off and only the notification is lost. | Mapping affects notice, not lifecycle | |
| PRT-089 | Remediation | Attempt sign-off with actions outstanding. | Sign-off is refused server-side. | Sign-off gating | Pass - with an action outstanding, sign-off was refused: "PRECONDITION: Only a completed remediation action can be signed off." (Round two reached only the parameter check; the state gate is now proven.) |
| PRT-090 | Remediation | Sign off as a T&C Supervisor with the attestation. | The case closes and the closure email is sent. | Sign-off and closure | Pass, checked twice because the first read was wrong. Driven in the portal as a real T&C Supervisor: Simunye Radingwana, holding only the `AL Portal - T&C Supervisor` web role, on case 900000001 which the adviser (Dev Account) had completed through the portal in PRT-087. The attestation is the "All remedial actions checked and approved?" control on the sign-off panel, and answering it Approved records it against the case. **The trap:** immediately after signing off, a normal page load still showed "All remedial actions checked and approved? **—**" and "Supervisor sign-off **—**", which reads exactly like the attestation never being stored. It is the Power Pages render cache, and the product warns about it in the very message it had just shown ("if it still shows the old state the site cache has not caught up"). Re-fetched with the cache defeated, the same page reads **"All remedial actions checked and approved? Yes"** and **"Supervisor sign-off (complete remedial task in IO): Approved, Simunye Radingwana, 21 Sep 2026"** - the attestation, the signatory by name and the date, which is what this row asks for. Recorded prominently because a supervisor who signs off and reloads WILL see the old state for a short while, and the natural reading of that is that their sign-off did not save. |
| PRT-091 | Remediation | Sign off choosing to move the case to recheck. | The case moves to recheck and the recheck email is sent. | Sign-off to recheck | Pass. Driven in the portal as a real T&C Supervisor: Simunye Radingwana, holding only the `AL Portal - T&C Supervisor` web role, on case 900000001 which the adviser (Dev Account) had completed through the portal in PRT-087. The adviser had answered **Recheck required? Yes** when completing, and the supervisor's approval honoured it: the case moved **Awaiting Sign-off -> Awaiting Recheck** (120910589 -> 120910590) rather than closing. The final outcome was deliberately left at "Leave for a separate regrade", and the page explains why that is the right choice here: "The final outcome is the recheck decision, so it is recorded here once every remedial action above has been signed off and the case reaches Awaiting Recheck." After the move the sign-off panel is replaced by a **Record the final outcome** panel, which is the correct next step and is the only action button left on the page. The routing half is the part worth recording: a **Recheck due** notification (event 120910806, template `RECHECK-DUE`, subject "Case 900000001 is waiting for its final outcome") was raised to the **T&C Manager resolved through `al_advisermapping`** - which is the first live proof that `TcManagerRouting` resolves a real mapping end to end, since it keys on the case's `al_adviseremail` and not on the adviser's name. The initial outcome is preserved throughout (BR-007): the page states "The initial outcome was Pass with issues. Recording a final outcome does not change it - both are kept." **The lifecycle was then closed out** (project owner asked for the recheck/final-outcome close, 2026-09-21): the Record the final outcome panel took **Pass** with a reason, and the case moved **Awaiting Recheck -> Closed** (120910590 -> 120910591). BR-007 is honoured in the data and not just in the wording - `al_outcome` `OUT-900000001-2` keeps `al_initialoutcome` 120910701 (Pass with issues) AND records `al_finaloutcome` 120910710 (Pass), with `al_regradedon`, `al_finalisedon` and the full `al_regradereason` stored. AD-031 too: an `al_auditevent` row `RegradeCase` names the actor **Simunye Radingwana**, the detail `Pass` and the reason in full. So the whole journey is now proved end to end on one case - Pass with issues, remediation raised, adviser completes, supervisor rejects, rework, supervisor approves, recheck, final outcome, closed - with an audit event naming an actor at every step. |
| PRT-092 | Remediation | Attempt sign-off as an Adviser. | The action is unavailable and refused server-side. | Sign-off permission | **Pass on the refusal, with the row's wording overtaken by a better behaviour.** The row expects the action to be "unavailable and refused server-side". It IS refused server-side: the portal's attestation path is a PATCH on the signed-in contact's `al_signoffrequest`, and as the adviser that returns **HTTP 403 `EntityPermissionWriteIsMissingDuringUpdate` - "You don't have permission to update contact table"**, which is the Power Pages table permission and not UI code. Confirmed nothing was written: the case is still Awaiting Sign-off and **zero `al_signoff` rows** were created. But the control is **not unavailable** - the page offers a "Sign off this remediation" panel with a decision list (Approved / Rejected), a regraded-outcome list and a notes box. Pressing it is where the design earns its keep: it answers **"Your portal account is not allowed to write a sign-off request, so nothing was recorded. The contact permission is bound to the reviewer and T&C Supervisor roles; ask an administrator to check your account rather than retrying."** That is a better outcome than hiding the control - it names the permission, the roles that hold it, and tells the person not to retry - so this is recorded as a pass against the intent with the row's letter flagged for the project owner to reword, rather than a fail against a product that behaves well. |
| PRT-093 | Remediation | Sign off a case already signed off. | The second sign-off is refused. | Double sign-off guard | Pass, on both halves - the control is withdrawn AND the server refuses, which is the distinction that matters. Driven in the portal as a real T&C Supervisor: Simunye Radingwana, holding only the `AL Portal - T&C Supervisor` web role, on case 900000001 which the adviser (Dev Account) had completed through the portal in PRT-087. **UI:** once the case is signed off and sitting at Awaiting Recheck, a cache-defeated fetch of the page shows the sign-off panel is gone from the markup entirely - no `ot-decision` select, no `data-ot-signoff-save` button - and the only action button left is "Record the final outcome". **Server:** hiding a control proves nothing, so the request was replayed by hand as Simunye, in her own authenticated session, with a valid `__RequestVerificationToken`, against the same action id - exactly the PATCH the page itself makes (`/_api/contacts(<her id>)` carrying the `al_signoffrequest` JSON that `SignoffRequestPlugin` parses). It was refused: **HTTP 400, "PRECONDITION: This remediation action has already been approved. Reopening an approved sign-off is a privileged correction (AD-031)."** Proved the negative: still exactly **two** `al_signoff` rows - the original rejection and the approval - and the case still at Awaiting Recheck. Two notes. The refusal reaches the wire wrapped as `UNEXPECTED: OutcomeTesting.Plugins.SignoffRequestPlugin could not complete. OrganizationServiceFault: ` in front of an otherwise clean sentence; the template's `friendlyError()` strips that before a person sees it, but it is the same raw-fault packaging flagged on APP-003 and is the same single decision for the project owner. Separately and positively: `al_CompleteRemediation` was found to refuse a **system administrator** outright - "Only the adviser who owns this remediation action can complete it" - so that command has no sysadmin break-glass, unlike `PermissionHelpers`. **UPDATE 2026-09-21 - the packaging flagged above is now FIXED and re-verified live.** The cause was worth more than the cosmetics: `PluginBase` rethrows an `InvalidPluginExecutionException` untouched, but **one command calling another never receives that exception** - it crosses the service boundary as `FaultException<OrganizationServiceFault>`, lands in the next catch down, and was re-labelled as an unexpected crash. So the message no longer STARTED with its prefix, and every client that branches on `PRECONDITION:`/`CONFLICT:`/`UNAUTHORIZED:` was reading the wrong thing; a rule working exactly as designed read as a broken product; and the plug-in class name went to whoever asked (NFR-OBS-01). `CommandHelpers.RefusalWithin` now takes the message from the first known prefix onwards - searching rather than matching the start, so it collapses nesting of ANY depth - and the full text still reaches the trace log and InnerException. Re-verified live in DEV after `pushassembly` (296,448 bytes, sha256 `a43f3886...`) by replaying the same request: the real PATCH the page itself makes, as Simunye, with a valid `__RequestVerificationToken`. It now returns exactly **"PRECONDITION: This remediation action has already been approved. Reopening an approved sign-off is a privileged correction (AD-031)."** - the prefix first, no class name, no `OrganizationServiceFault`. Worth recording that the portal's `friendlyError` already tested the refusal prefixes BEFORE `UNEXPECTED:`, which is why a person saw clean wording while the wire did not - the client was compensating for a server fault, and that compensation is now unnecessary rather than load-bearing (799 app tests and all portal templates re-checked). |
| PRT-094 | Remediation | Check the adviser's remediation email. | It uses the admin-edited wording with the correct tokens resolved. | Editable remediation letter | Pass, checked against the STORED template rather than against how the letter reads, which is what this row actually asks. The remediation letter for case 900000001 is `al_notification` event **Remediation assigned**, status Sent, and its body is stored template **`REMEDIATION-PASS-WITH-ISSUES`** (`al_notificationtemplate`, last edited 2026-09-20T19:49:54Z - an administrator edit, not a code default). Every token resolved and **no literal `{{...}}` survived anywhere in subject or body**: `{{reference}}` -> 900000001 in the subject "Remedial needed - Pass with issues: 900000001"; `{{adviser}}` -> "Dear Simunye Radingwana,"; `{{client}}` -> "Test Client Alpha"; `{{grading}}` -> "a pass with issues grading"; `{{dueText}}` -> " It is due by 2 October 2026." matching the action's `al_duedate` of 2026-10-02; and the call-to-action rendered as a styled "Confirm remedial action" anchor carrying the correct case id. A second template was confirmed incidentally: a manually raised action with no grading selected **`REMEDIATION-OTHER`** and produced "Remediation required on case 900000003", so the template CHOICE varies with the reason and is not hard-coded to one letter. Recipient resolution was also settled here - `NotificationEmitterPlugin` fires on **Create** of `al_remediationaction` and addresses the **assigned contact**, not the case's `al_adviseremail`, so the letter and the work go to the same person. Two honest scope notes. (a) Case 900000001 currently has `al_advisername` = Dev Account but `al_adviseremail` = Simunye's address; that divergence is a hand-made fixture for the role tests - every other case in DEV has the two agreeing - and is NOT a product fault. (b) Reassigning an existing action emits no new letter, because the step is on Create; `Remediation.Reassign`'s own comment says reassignment should tell the new owner (PP-15), so whether that gap matters is worth a ruling, but it was not reachable to test here without disturbing the sign-off fixture. |
| PRT-095 | Profile | Open the profile page. | The signed-in contact's own details are shown. | Profile page | Pass. `/profile` as the Dev Account renders the Power Pages profile form for the **signed-in contact** - heading "Profile", the contact's own name, and their own first name, last name, email, phone, organisation, title, nickname, website and preferred language fields. No other contact's details appear. Note this is the stock Power Pages profile page rather than an Outcome Testing screen, so what it shows is the contact record the sign-in resolved to, which is the point of the row. |
| PRT-096 | Profile | Attempt to view another contact's profile by id. | Access is refused. | Profile scoping | Pass, and refused by more than one route. As the Dev Account (`svc.automate.aq-dev`), holding only the `AL Portal - Adviser Remediation` web role and no System Administrator, `/profile/?id=<another contact's guid>` and `/profile/?contactid=<same>` both render the **signed-in contact's own** record - First Name Dev, Last Name Account, e-mail svc.automate.aq-dev@... - with the parameter ignored entirely rather than honoured or errored. That is the stock Power Pages profile page, which resolves the contact from the authentication session and not from the URL, so there is no id for an attacker to change. Probed the harder route as well, because a page ignoring a parameter is not the same as the data being protected: the portal Web API returns **HTTP 403 `EntityPermissionReadIsMissing`** - "You don't have permission to read the contact table" - for `/_api/contacts(<other guid>)` AND for an unfiltered `/_api/contacts` list, so this role holds no contact read permission at all. Consistent with PRT-092, where the same table refused a WRITE for the same reason. |
| PRT-097 | Access Denied | Open a page the role may not see. | The Access Denied page is shown, not a raw error. | Access denied handling | Pass, on three different pages and by ADDRESS rather than by a link. Driven in the portal as a real second identity: the Dev Account (`svc.automate.aq-dev`), holding **only** the `AL Portal - Adviser Remediation` web role and no System Administrator, with `REM-900000001-2` on case 900000001 assigned to its contact. Gate 0 had to be opened first - a private Power Pages site refuses everyone not on Manage access, and the account stopped bypassing it the moment System Administrator came off, which is the portal-access runbook's own warning that an admin account is a useless test subject. `/tax-reviews`, `/aqs-reviews` and a direct `/review?id=89a4bf3e-...` (a real review, assigned to this very contact, on a case they are the AQS checker for) each returned **HTTP 403** and rendered the site's own **Access Denied** page - not a raw error, not a stack trace, and not a blank. The wording is worth quoting because it covers both reasons without guessing between them: "You do not have access to this page. This may be because the page is restricted to a different role, or because the record is not one you work on. If you think you should have access, contact your Outcome Testing manager." It also names who they are signed in as, which is what stops a person chasing a permission problem that is really a wrong-account problem. |
| PRT-098 | Page Not Found | Open an unknown URL. | The Page Not Found page is shown. | Not found handling | Pass. An unknown address returns a real **HTTP 404** - the status code, not a 200 dressed up as an error page - and renders the site's own Page Not Found: heading "We could not find that page", then "The address may be mistyped, or the page may have moved. Nothing you were working on has been lost." Site chrome, header and navigation are intact, and there is no stack trace, no platform error and no blank page. Two recovery links are offered, Back to My Work and Case search, so the page is a way out rather than a dead end. The one console error is the 404 itself being logged by the browser, not a script fault. |
| PRT-099 | Web API | Call `/_api/al_outcomecases` as an Adviser. | No case data is returned. | Case table endpoint closed | Pass — /_api/al_outcomecases returns 302 to sign-in unauthenticated. |
| PRT-100 | Web API | Call `/_api/al_responses`. | The endpoint returns nothing. | Response endpoint closed | Pass — /_api/al_responses returns 302 to sign-in unauthenticated. |
| PRT-101 | Web API | Call `/_api/al_caseassignments`. | The endpoint returns nothing. | Assignment endpoint closed | Pass — /_api/al_caseassignments returns 302 to sign-in unauthenticated. |
| PRT-102 | Web API | Call `/_api/al_remediationactions` as an unrelated contact. | No other adviser's actions are returned. | Remediation endpoint scoping | Pass — /_api/al_remediationactions returns 302 to sign-in unauthenticated. |
| PRT-103 | Web API | Attempt an invalid lifecycle transition directly. | The transition is refused server-side. | Lifecycle gating outside the UI | |
| PRT-104 | Table permissions | Review the four case tables' permissions. | Authenticated Users holds no read on any of them. | Global read removed | Pass — the four case tables bind to seven job roles and Administrators; Authenticated Users holds none. |
| PRT-105 | Table permissions | Review reference table permissions. | Question, section and route reads remain available. | Reference data readable | Pass — question, question version, section, route and fail reason reads remain in place. |
| PRT-106 | Table permissions | Withdraw a user's web role and reload. | They immediately lose access to the scoped data. | Role withdrawal takes effect | Pass on the row as written, with an asymmetry worth recording. Removed the single web role `AL Portal - Adviser Remediation` from the Dev Account's contact in Dataverse while its portal session was live and mid-journey, then reloaded **without signing out**. Access was gone on the very next request - no grace period, no cached page served: `/`, `/cases`, `/remediation`, the case-scoped `/remediation?case=...`, `/tax-reviews` and `/profile` all returned **HTTP 403** with the Access Denied page, the navigation collapsed to Sign out alone, and the response bodies carried no case reference, client name or adviser response text. So withdrawal is immediate, which is what the row asks. **The asymmetry:** putting the role BACK did not restore access in the same session - the portal kept refusing after the intersect row was re-created and confirmed present in Dataverse. That is the safe direction (it fails closed, and a stale cache that over-grants would be the serious bug), and the product already explains it on the refusal page itself: "If a role you were granted is not in that list, the portal has not picked it up for this session yet. Sign out and sign back in." Recorded so that nobody reads a restored-but-still-refused account as a permissions fault - it needs a fresh sign-in, and the page says so. **Correction, later the same day:** the note above said restoring a role needs a fresh sign-in. That is not right, and the distinction matters to anybody debugging a role change. Re-adding web roles and waiting a few minutes DID restore access in the same session, with no sign-out - the pages went from 403 back to 200 on their own. So the cache is **time-based, not sign-in-gated**: withdrawal is immediate, restoration lags by the cache lifetime, and signing out and in is a way to skip the wait rather than a requirement. The direction is still the safe one and the refusal page's advice is still good advice; it is the reason that was wrong. |
| PRT-107 | All pages | Leave the session idle past the timeout. | Re-authentication is required. | Session timeout | |
| PRT-108 | All pages | Open the portal in each supported browser. | Layout and function are correct in all of them. | Browser compatibility | |
| PRT-109 | All pages | Open the portal at phone and tablet widths. | Layout adapts with no horizontal scrolling. | Responsive layout | |
| PRT-110 | All pages | Navigate with the keyboard and a screen reader. | All controls are reachable and labelled. | Accessibility | |
| PRT-111 | All pages | Have two users edit the same case at once. | The second save is rejected or merged, never silently lost. | Concurrent edit handling | |
| PRT-112 | Deployment | Upload the web templates and files to TEST. | The portal matches DEV after a diff before upload. | Portal configuration promotion | |
| PRT-113 | Deployment | Re-check the four table permissions after install. | All four are bound to job roles, not Authenticated Users. | Permission promotion | |
| PRT-114 | Deployment | Run the page permission steps in TEST. | Admin and due date pages are reachable by the intended roles. | Page permissions are data | |
| PRT-115 | Deployment | Confirm Q-TAX-04 exists in TEST. | The Tax notes panel shows content. | Checklist content is data | |
| PRT-116 | Deployment | Confirm adviser mapping rows exist in TEST. | T&C Managers are notified of waiting sign-offs. | Mapping rows are data | |
| F45 | The adviser had nowhere to write, and the button said nothing | The primary adviser journey was a silent dead end for the ordinary Pass with issues case. `OT Remediation` renders the per-issue response editor only inside `{% if ot_count > 0 %}` - the branch taken when the action's description carries `- ` bullet lines, which it does only when the checker marked specific non-pass items. `Remediation.RaiseFor` deliberately raises an action WITHOUT a list when there is nothing to list, and says so in its own comment: "No item list is not a reason to raise nothing: a grade can require remediation with no pass/fail test point behind it". Those actions fell to the `{% else %}` branch, whose cell was `{{ a.al_adviserresponse | default: '-' }}` - **read-only, whatever the assignment and whatever the status**. Found in DEV on 2026-09-21 as the Dev Account, holding only `AL Portal - Adviser Remediation`, on `REM-900000001-2`: assigned to them, Open, correct role, every condition for editing met, and no field on the page. Then the second half: the submit handler builds its payload from the `data-ot-response` cells, and `send()` opened with `var pending = rows(); if (!pending.length) { return; }` - **a bare return**. Pressing "Save and sign off this remediation" issued no request at all and reported neither success nor failure; verified in Dataverse, `modifiedon` unchanged. The three case-level answers were discarded too, because they ride on the per-row payload. And `al_CompleteRemediation` refuses a blank response by design, so even a firing button would have been refused. Net effect: an adviser on a Pass with issues case could not complete remediation through the portal by any route, the case could not leave Awaiting Remediation by their hand, and nothing on screen said so. | **Fixed** (this run) - the `{% else %}` branch renders the same editable cell as the itemised branch, under the same condition (`user and a.al_assignedcontactid.id == user.id and a.al_actionstatus.value != 120910602`), and the bare return is replaced by a message naming what is missing. The silent return is kept rather than deleted, deliberately: with the cell fixed it should be unreachable for an assigned open action, so if it fires again it is a symptom - and a button that does nothing in silence is exactly how the first fault hid. Deployed with `pushwebtemplate` (one template, 119,462 -> 122,847 chars) rather than a site upload, which would have pushed stale local config over DEV. Verified live on the deployed page: the textarea renders, an empty submit is refused by name (PRT-080), a written response completes the action, and the case moves to Awaiting Sign-off with the T&C Manager notified (PRT-087). High - no data was lost or mis-saved and the server never wavered, but the one thing the adviser's page exists to collect could not be collected, on the commonest remediation there is |
| F48 | A malformed case id puts a platform exception on the page | `OT Case Detail` renders `Liquid error: Exception has been thrown by the target of an invocation.` above its own empty state when the `id` parameter is not a GUID - for example `?id=not-a-guid`. A well-formed id that matches nothing is handled perfectly ("Case not available"), and the friendly state still appears underneath the error, so nothing is lost and nothing beyond the exception text leaks. Found 2026-09-21 running PRT-038. Same family as the refusal packaging fixed earlier this run: a platform fault string reaching a reader who can do nothing with it. | **Not fixed** - recorded for the project owner. Low: no data is disclosed, the page still renders, and reaching it needs a hand-edited URL. The fix is to guard the id before the fetch reads it, the same shape as the guard already used for a missing row **UPDATE 2026-09-21 - FIXED and verified live.** The id is now checked by SHAPE before the fetch reads it, because asking Dataverse is the thing that throws: dashes and the braced form are stripped, every hex digit is removed, and what survives must be empty with exactly 32 hex characters. An id that fails is treated as no id at all, so the page falls into the state it already has for arriving without one - deliberately, rather than a message of its own, so that someone who mistyped a link and someone probing for ids are shown the same thing and neither learns whether an id was malformed or merely unknown. **Applied to three pages, not the one this row found.** `OT Review Detail` (both `id` and the claim-by-case `case`) and `OT Remediation` (`case`) take ids exactly the same way and had exactly the same exposure; a fix to Case Detail alone would have left the same exception two clicks away. Verified live as Simunye on all three: `/case-details?id=not-a-guid`, `/review?id=not-a-guid`, `/review?case=not-a-guid&type=aqs` and `/remediation?case=not-a-guid` all render the page's ordinary empty state with **no Liquid error** in the body. The regression that mattered was checked too, and is the reason this is a pass rather than an assertion: a well-formed id matching nothing still answers "Case not available", and real case 900000003 still renders in full. See `docs/deployment/2026-09-21-three-findings-fixed.md`. |
| F49 | The AQS checker sees the Tax checker's notes only after they no longer need them | `OT Tax Notes` exists to put the Tax check's Q-TAX-03 note and Q-TAX-04 remedial text in front of the two people AD-020 filters it away from - the adviser reading a remediation and the AQS checker reading the case. It is written correctly and included in both places. But on the review page the include is guarded by `{% if rv.al_submittedon and rv.al_reviewtype.value != 120910200 %}`, and `rv` is the review BEING VIEWED - so the panel appears on an AQS review only once the AQS checker has already submitted it, which is after the only moment it could have changed their assessment. The guard's stated reason is about the TAX note still being its author's to change, and the panel's own fetch already restricts itself to submitted Tax reviews, so the condition is guarding the wrong review. Proved on case 900000003, whose submitted Tax review stores `al_answerrichtext` = `<strong>Bold kept</strong> plain text link` against Q-TAX-04 "Tax Remedial": its AQS review, Assigned and not yet submitted, renders no panel at all. This is the third time this panel's wiring has defeated its purpose - audit finding 14 was the same panel fetching the wrong question code, and failing silently for the same reason, because an empty answer is how it is told there is nothing to show. | **Not fixed** - recorded, because WHEN a checker may see another discipline's notes is a project-owner decision (2026-09-19) rather than an obvious bug, and the change is one condition. Medium-High: no data is wrong and nothing is mis-saved, but from 2026-09-20 a Tax fail is carried into ONE combined remediation raised after the AQS check, which makes the AQS checker the person the Tax checker is writing for **UPDATE 2026-09-21 - FIXED and verified live.** `rv.al_submittedon` is removed from the include's guard; the discipline test `rv.al_reviewtype.value != 120910200` stays. The guard's stated reason - that a Tax note is still its author's to change before submission - is **fully preserved and was never the thing at risk**: `OT Tax Notes` already restricts its own fetch to `al_reviewtype eq 120910200` AND `al_submittedon not-null`, so it has always read submitted Tax reviews only. The condition was guarding the right thing on the wrong review. Verified live on case 900000003's AQS review, and the review's own state was **queried rather than inferred** because the page mentions the word "submitted" three times in the panel itself and a careless read of the page text would have passed this for the wrong reason: `al_submittedon` is null and `al_reviewstatus` is 120910210 Assigned. With the AQS check still open, the panel now renders the Tax checker's Q-TAX-04 text (the `<strong>Bold kept</strong>` answer this row was proved against). Recorded as a project-owner decision when raised; taken as directed ("fix the findings") on the ground that the condition was not expressing that decision but misapplying it to a different record. |
| F50 | A failed case can be graded with nobody carrying the fail | Case 900000004 was failed on both legs and came out **Potential harm with all four accountability flags False** and both accountable-contact lookups null. Nothing refused the submission. Two causes, both proved on 2026-09-21. **The recorded judgement is discarded on the deferred-Tax path:** the "Who carries this fail" panel was used on the Tax leg and saved - the Tax review still holds `al_pendingaccountability` with `fqAdviser: true` - but a Tax fail on a Tax-then-AQS case raises no outcome (it defers, correctly), and `SubmitReviewPlugin` applies accountability by reading `al_pendingaccountability` from the review BEING SUBMITTED. That is the AQS review, which had none, so `AccountabilityRequestPlugin.ApplyParked` returned false and the Tax checker's answer is still sitting on the Tax review unread. **And there is no fallback:** the panel promises "Left alone, the extract names the people the case itself does - the paraplanner for a File Quality fail, the adviser for an Advice Quality fail", and no such default exists; the booleans simply keep Dataverse's False. The same class as F43, F44 and F49 this session: the thing that makes the design safe was never connected. | **Not fixed** - recorded. The choice is a project-owner one: refuse the submission (which is what the checklist expects), implement the fallback the UI already promises, or carry parked accountability forward from a deferred leg to the outcome the combined submit creates. The third is the narrow fix and would also stop a saved judgement being silently dropped. **High** for a product whose purpose is recording accountability: no data is wrong and nothing is mis-saved, but a Potential harm case can reach remediation, sign-off and closure with nobody named as carrying it, and a checker who DID name somebody is never told their answer was dropped **UPDATE 2026-09-21 - CORRECTED, then FIXED.** **The correction first, because it changes what this finding is.** The second half above - "there is no fallback... no such default exists" - is **wrong**. The panel promises that *the extract* names the people the case itself does, and the extract does exactly that: `GenerateExportPlugin.IsAccountable` derives the pair whenever all four flags are false ("nobody has said yet", not "nobody is responsible"), and `app/src/features/cases/failAccountability.ts` is a deliberate client-side mirror of it so the case screen shows the derived pair rather than four empty boxes - both carrying the project owner's 2026-09-16 decision, both documenting the retired OD-024 gate. So case 900000004 is NOT a Potential harm case with nobody carrying it: its extract names the adviser for the Advice Quality fail. The severity claim "a case can reach closure with nobody named as carrying it" does not stand, and I found this by reading the export path before writing the fix rather than after. **What was real** is the first half, and it is worse than the row gave it credit for: the Tax checker DID use the panel and name somebody, and their answer was discarded - and the derivation that replaced it names a DIFFERENT person. The derivation gives the **paraplanner** for a File Quality fail; this Tax checker had named the **adviser**. So the judgement was not merely lost, it was silently replaced with an answer about somebody else, which is the part that matters for a product whose purpose is recording accountability. **Fixed** by the narrow option this row recommended: `SubmitReviewPlugin.ParkedAccountability` now answers which review holds the judgement belonging to the outcome being created - the review being submitted whenever it has one of its own, so nothing recorded about THIS submission is ever overwritten, and otherwise the most recently submitted active review on the same case that carries one. Whichever it came from is the row cleared afterwards. **Deliberately not decided:** where BOTH legs recorded a judgement, this leg wins and the other is left parked rather than merged - merging a File Quality pair from one leg with an Advice Quality pair from another is a rule nobody has given, and inventing one would put a combination in the extract that no checker chose. That case still needs a ruling. **Proved by reverting, twice, and the first attempt is worth recording because it failed in exactly the way this finding describes:** five unit tests around `ParkedAccountability` stayed GREEN with the call site reverted, because they proved the lookup and not that anything called it. `TaxFailReachesAqsTests.The_tax_checkers_judgement_reaches_the_outcome_the_aqs_submit_creates` now drives the real `Submit` through both legs and asserts the flags on the created `al_outcome`; with the call site reverted that one test fails and only that one. 1,385 -> 1,391 tests. Deployed to DEV (296,960 bytes, sha256 `f9fb47fb...`). **Verified live in DEV on the real product, not only in tests** - the whole journey on case 900000006 (Tax then AQS), driven through the portal as Simunye Radingwana: the Tax check failed, the "Who carries this fail" panel opened, **the adviser** was ticked for File Quality and parked as `fqAdviser:true`; the Tax submit deferred exactly as designed - case back to **Queued** (120910583), `al_taxoutcome` Fail, **zero** `al_outcome` rows, judgement still parked - which is precisely the state the old code dropped it from. The AQS leg was then allocated, graded **Pass** with its own accountability panel correctly hidden (so nothing was parked on the AQS review, the exact scenario), and submitted. Result: `OUT-900000006-2` carries **`al_fqadviseraccountable: true`** and `al_fqparaplanneraccountable: false`. That pairing is the proof and not merely a pass - the DERIVATION for a File Quality fail names the paraplanner, so a record naming the adviser can only have come from the Tax checker's carried-forward judgement. The Tax review's `al_pendingaccountability` is now **null**, cleared on the review it was read from. The rest of the lifecycle was checked rather than assumed: the case is at Awaiting Remediation and `REM-900000006-2` was raised reading "(Pass; Tax check: Fail)", so the combined-remediation behaviour PRT-067 proved is unchanged by this fix. Fixture note: 900000006's Tax review was reassigned from Dev Account to Simunye via `al_AssignCase` to drive this, and the case carries the service-account address so its lifecycle mail reached no person. |
